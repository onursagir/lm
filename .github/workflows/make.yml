name: Make
on:
  push:
    branches:
      - main
jobs:
  build-html:
    runs-on: ubuntu-latest
    steps:
      - uses: onursagir/regels.overheid.nl/.github/actions/testing@main
        with:
          name: 'De Legitimmaat'
          pat: secrets.GITHUB_TOKEN